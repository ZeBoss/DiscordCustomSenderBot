# DiscordCustomSenderBot


A discord bot that allows users to register a name and avatar and send messages using that via a webhook.

Current commands
/speak [input string]
/register [name string] [avatar_url string]